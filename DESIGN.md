# LAVA Design System

이 문서는 루트에 있는 과거 프로토타입 UI 이미지를 바탕으로 LAVA의 제품 디자인 방향과 구현 기준을 정리합니다.

## 기준 화면

| 파일 | 화면 | 디자인에서 읽어야 할 것 |
| --- | --- | --- |
| [home.png](./docs/assets/prototypes/home.png) | 대시보드 홈 | 앱 셸, 사이드바, 카드, 캘린더, 최근 메시지 |
| [project-generate.png](./docs/assets/prototypes/project-generate.png) | 새 프로젝트 생성 최종 확인 | 단계형 생성 플로우, 확인 카드, 주요 CTA |
| [project-home.png](./docs/assets/prototypes/project-home.png) | 프로젝트 홈 | 프로젝트 헤더, 산출물 카드, 외부 링크 버튼 |
| [project-fsd.png](./docs/assets/prototypes/project-fsd.png) | 기능 명세서 편집 | 문서 편집 레이아웃, 탭, 저장 상태, AI 어시스턴트 패널 |

## 제품 인상

LAVA는 학생 팀 프로젝트와 주니어 개발자를 위한 작업형 협업 도구입니다. 화면은 마케팅 사이트처럼 장식적이면 안 되고, 반복해서 들어와 확인하고 수정하는 운영 도구처럼 차분해야 합니다.

핵심 인상은 아래 세 가지입니다.

- 빠르게 시작한다: 주요 CTA와 AI 생성 상태는 눈에 잘 띄어야 합니다.
- 문서를 믿고 수정한다: 산출물 문서 영역은 종이 문서처럼 읽기 좋고 안정적이어야 합니다.
- AI가 옆에서 돕는다: AI는 화면 전체를 덮는 챗봇이 아니라, 문서와 일정 옆에서 보조하는 패널로 배치합니다.

## 레이아웃 원칙

### 앱 셸

- 데스크톱 기본 해상도는 1440px 이상을 우선합니다.
- 좌측 사이드바는 고정 폭 260px을 기준으로 합니다.
- 상단 헤더는 64px 또는 70px 높이를 사용합니다.
- 콘텐츠 영역은 좌우 40px, 상하 32px 이상의 여백을 둡니다.
- 대시보드와 프로젝트 홈은 넓은 화면에서 2컬럼 또는 3컬럼 구성을 허용합니다.
- 문서 편집 화면은 `사이드바 + 본문 + 우측 AI 패널`의 3영역 구조를 기본으로 합니다.

### 콘텐츠 밀도

- LAVA는 작업 도구이므로 정보 밀도는 중간 이상으로 유지합니다.
- 카드 간격은 넓게 두되, 불필요한 장식 섹션은 만들지 않습니다.
- 화면 첫 진입에서 현재 위치, 프로젝트 상태, 다음 행동이 보여야 합니다.

## 컬러 토큰

프로토타입의 핵심 색은 오렌지-레드입니다. 브랜드의 활력은 버튼, 선택 상태, 경고성 강조에만 사용하고 배경 대부분은 흰색과 연회색으로 유지합니다.

| 토큰 | 값 | 용도 |
| --- | --- | --- |
| `color.brand.primary` | `#FF5A2D` | 주요 CTA, 활성 내비게이션, 진행 단계 |
| `color.brand.primaryHover` | `#F04A23` | 주요 CTA hover |
| `color.brand.red` | `#E6002D` | AI 강조, 알림 배지, 중요 텍스트 |
| `color.brand.warmBg` | `#FFF1EC` | 활성 메뉴 배경, 오렌지 아이콘 배경 |
| `color.bg.app` | `#F5F6F8` | 프로젝트 홈, 문서 편집 작업 배경 |
| `color.bg.surface` | `#FFFFFF` | 카드, 헤더, 사이드바, 문서 페이지 |
| `color.border.default` | `#E5E7EB` | 카드/패널 경계선 |
| `color.border.strong` | `#D8DCE2` | 입력창, 큰 컨테이너 경계선 |
| `color.text.primary` | `#2F2F33` | 제목, 주요 본문 |
| `color.text.secondary` | `#6F737A` | 설명, 메타 정보 |
| `color.text.muted` | `#A0A4AB` | placeholder, 비활성 텍스트 |
| `color.status.success` | `#35B85A` | 완료, 동기화 정상 |
| `color.status.warning` | `#F5A400` | 진행중, 대기 |
| `color.status.purple` | `#7B61FF` | 스프린트, 기능 명세 계열 보조색 |
| `color.status.blue` | `#5865F2` | Discord, 외부 서비스 계열 |

## 타이포그래피

기본 폰트는 `Pretendard`를 우선하고, 없으면 시스템 sans-serif를 사용합니다.

| 토큰 | 크기/행간 | 굵기 | 용도 |
| --- | --- | --- | --- |
| `text.display` | 36/46 | 700 | 문서 제목, 큰 편집 문서 제목 |
| `text.title` | 28/38 | 700 | 프로젝트 제목 |
| `text.section` | 22/30 | 700 | 섹션 제목 |
| `text.cardTitle` | 18/26 | 700 | 카드 제목 |
| `text.body` | 14/22 | 400 | 일반 본문 |
| `text.bodyStrong` | 14/22 | 600 | 목록 제목, 강조값 |
| `text.caption` | 12/18 | 400 | 메타 정보, 보조 라벨 |

규칙:

- letter spacing은 기본값 `0`을 사용합니다.
- 한글 본문은 줄간격을 충분히 두고, 긴 설명은 2줄 안에 들어오게 줄입니다.
- 버튼 안 텍스트는 14px, 600 weight를 기본으로 합니다.

## 간격과 형태

| 토큰 | 값 | 용도 |
| --- | --- | --- |
| `space.1` | 4px | 아이콘과 텍스트 사이 |
| `space.2` | 8px | 작은 요소 간격 |
| `space.3` | 12px | 폼 내부 간격 |
| `space.4` | 16px | 카드 내부 기본 간격 |
| `space.5` | 20px | 리스트 아이템 간격 |
| `space.6` | 24px | 카드 패딩 |
| `space.8` | 32px | 섹션 간격 |
| `space.10` | 40px | 페이지 좌우 여백 |

| 토큰 | 값 | 용도 |
| --- | --- | --- |
| `radius.sm` | 6px | 입력창, 작은 태그 |
| `radius.md` | 8px | 카드, 패널, 리스트 아이템 |
| `radius.lg` | 12px | 주요 CTA, 검색창 |
| `radius.pill` | 999px | 상태 배지, 알림 카운트 |

카드는 기본적으로 8px radius를 사용합니다. 프로토타입의 부드러운 느낌은 큰 radius보다 여백, 그림자, 밝은 배경색으로 재현합니다.

## 그림자

| 토큰 | 값 | 용도 |
| --- | --- | --- |
| `shadow.card` | `0 8px 24px rgba(16, 24, 40, 0.06)` | 대시보드 카드 |
| `shadow.float` | `0 12px 32px rgba(16, 24, 40, 0.10)` | 우측 패널, 강조 CTA |
| `shadow.none` | `none` | 입력창, 사이드바 |

그림자는 과하게 쓰지 않습니다. 같은 화면 안에서 떠 있는 것처럼 보여야 하는 요소는 CTA, 요약 카드, AI 패널 정도로 제한합니다.

## 컴포넌트 기준

### 사이드바

- 폭: 260px
- 배경: 흰색
- 우측 경계선: `color.border.default`
- 로고는 상단 좌측에 40px 내외 크기로 둡니다.
- 메뉴 아이템은 아이콘 + 라벨 조합을 사용합니다.
- 활성 메뉴는 `color.brand.warmBg` 배경과 `color.brand.primary` 텍스트를 사용합니다.
- 사용자 프로필은 하단에 고정 배치합니다.

메뉴 순서:

1. 대시보드
2. 내 프로젝트
3. AI 어시스턴트
4. 캘린더
5. 설정

### 상단 헤더

- 좌측에는 페이지 제목 또는 breadcrumb를 둡니다.
- 중앙 검색은 대시보드에서만 사용합니다.
- 우측에는 알림, 메시지, 멤버, 주요 CTA를 둡니다.
- 알림 카운트는 빨간 pill 배지로 표시합니다.

### 버튼

| 유형 | 스타일 | 사용처 |
| --- | --- | --- |
| Primary | 오렌지-레드 배경, 흰색 텍스트 | 프로젝트 생성, 저장 |
| Secondary | 흰색 배경, 회색 border | 이전 단계, 외부 링크 |
| Ghost | 투명 배경, 회색 텍스트 | 보조 액션, 이력 보기 |
| Danger | 빨간 배경 또는 빨간 텍스트 | 삭제, 위험 액션 |

규칙:

- 주요 버튼에는 아이콘을 함께 쓸 수 있습니다.
- 아이콘은 lucide 계열을 기준으로 합니다.
- 위험 액션은 바로 실행하지 않고 확인 모달 또는 재인증 단계를 거칩니다.

### 카드

- 배경: 흰색
- border: `1px solid color.border.default`
- radius: 8px
- padding: 24px
- 제목, 설명, 메타 정보, 액션 영역을 명확히 분리합니다.
- 카드 안에 다시 카드 형태를 중첩하지 않습니다.

카드 유형:

- 지표 카드: 큰 숫자와 작은 설명을 조합합니다.
- 프로젝트 리스트 아이템: 아이콘, 프로젝트명, 유형, 기간, 상태, 더보기 액션을 한 줄에 배치합니다.
- 산출물 카드: 아이콘, 문서명, 설명, 최근 수정 시각, 열기 버튼을 포함합니다.
- AI 요약 카드: 생성된 산출물과 완료 상태를 pill 형태로 표시합니다.

### 상태 배지

| 상태 | 색상 | 예시 |
| --- | --- | --- |
| 완료 | green | AI 명세 완료 |
| 진행중 | yellow/orange | 진행중 |
| 스프린트 | purple | 스프린트 1주차 |
| 알림 | red | 알림 카운트 |
| 보류 | gray | 초대 대기 |

배지는 pill 형태를 사용하고, 텍스트는 짧게 유지합니다.

### 단계형 진행 표시

프로젝트 생성 플로우는 가로 stepper를 사용합니다.

단계:

1. 기본 정보
2. 멤버 초대
3. AI 명세 생성
4. 최종 확인

규칙:

- 완료 단계는 원형 체크 아이콘으로 표시합니다.
- 현재 단계는 오렌지 원형 숫자와 굵은 라벨로 표시합니다.
- 단계 사이 연결선은 오렌지로 채웁니다.
- 개인 프로젝트에서는 멤버 초대 단계를 건너뛰거나 비활성 단계로 표시합니다.

### 캘린더

- 월 단위 미니 캘린더와 전체 일정 캘린더를 모두 지원할 수 있게 설계합니다.
- 프로젝트별 색상 구분이 필요합니다.
- 마감 기한만이 아니라 모든 일정 항목을 표시합니다.
- 완료 상태나 진행률은 표시하지 않습니다.

### 문서 편집기

문서 편집 화면은 LAVA의 핵심 화면입니다.

구조:

- 상단 breadcrumb: `내 프로젝트 > 프로젝트명 > 문서명`
- 문서 탭: API 명세, 기능 명세, 일정표
- 중앙 문서 페이지: 흰색 종이 느낌의 편집 캔버스
- 우측 AI 어시스턴트 패널: 문서 보완 요청과 응답 표시
- 상단 우측 상태: 실시간 동기화, 참여자, 저장 버튼, 더보기 메뉴

문서 페이지:

- max-width는 760px에서 820px 사이를 권장합니다.
- 배경은 흰색, 주변 작업 영역은 `color.bg.app`를 사용합니다.
- 문서 제목은 크게, 섹션 제목은 굵게 표시합니다.
- 선택된 문장이나 AI가 제안한 변경 위치는 오렌지 밑줄 또는 왼쪽 강조선으로 표시합니다.

### AI 어시스턴트 패널

- 우측 고정 패널 폭은 320px에서 360px을 기준으로 합니다.
- 헤더에는 `LAVA AI 어시스턴트`와 닫기 버튼을 둡니다.
- 사용자 메시지는 `color.brand.warmBg` 계열 배경을 사용합니다.
- AI 응답은 흰색 카드와 얇은 border를 사용합니다.
- 문서 반영 액션은 오렌지 텍스트 버튼으로 둡니다.
- 하단 입력창은 항상 고정하고 전송 버튼은 원형 오렌지 아이콘 버튼을 사용합니다.

## 화면별 설계 기준

### 대시보드 홈

- 상단에는 환영 문구, 검색, 알림, 새 프로젝트 생성 CTA를 둡니다.
- 좌측 메인 컬럼에는 진행 프로젝트 지표와 최근 프로젝트 목록을 둡니다.
- 우측 컬럼에는 캘린더와 최근 메시지를 둡니다.
- 지표 카드는 3개 이하로 유지합니다.

### 새 프로젝트 생성

- 전체 흐름은 단계형 wizard로 구성합니다.
- 최종 확인 화면에서는 프로젝트 기본 정보와 AI 생성 요약을 나란히 확인합니다.
- 생성 CTA는 우하단에 강하게 배치합니다.
- 이전 단계 버튼은 좌하단에 약하게 배치합니다.
- AI 동기 처리 중에는 로딩 상태와 실패 fallback 문구 적용 여부를 명확히 보여줍니다.

### 프로젝트 홈

- 상단 프로젝트 헤더는 프로젝트명, 상태, 현재 스프린트, 기간, 팀원 수, AI 문서 생성 상태를 포함합니다.
- Discord, Github, Notion 같은 외부 링크는 우측 버튼으로 둡니다.
- 산출물 문서는 API 명세서, 기능 명세서, 개발 일정표 3개 카드로 보여줍니다.

### 문서 편집

- 탭으로 API 명세, 기능 명세, 일정표를 전환합니다.
- 기능 명세서와 API 명세서는 모든 멤버가 직접 수정할 수 있습니다.
- 일정 직접 수정과 일정 AI 수정은 리더만 사용할 수 있습니다.
- 저장 버튼은 항상 우상단에 배치합니다.
- AI 패널은 문서 맥락을 유지한 채 보조하는 영역으로 둡니다.

## 반응형 기준

### 데스크톱

- 사이드바 고정
- 대시보드 2컬럼
- 프로젝트 산출물 3컬럼
- 문서 편집은 우측 AI 패널 표시

### 태블릿

- 사이드바는 접힘 상태를 허용합니다.
- 프로젝트 산출물은 2컬럼으로 줄입니다.
- AI 패널은 오버레이 패널로 전환할 수 있습니다.

### 모바일

- 사이드바는 하단 내비게이션 또는 drawer로 전환합니다.
- 카드와 문서 산출물은 1컬럼으로 표시합니다.
- 문서 편집과 AI 패널은 탭 전환 구조로 분리합니다.

## 접근성과 상태

- 모든 인터랙티브 요소는 키보드 포커스 상태를 가져야 합니다.
- 색상만으로 상태를 구분하지 말고 텍스트 또는 아이콘을 함께 사용합니다.
- 주요 CTA와 배경의 대비는 WCAG AA 수준을 목표로 합니다.
- 로딩, 성공, 실패, 비활성 상태를 각 컴포넌트에서 명확히 정의합니다.
- AI 호출 실패 시 기존 입력과 문서는 유지하고, fallback 문구를 보여줍니다.

## 문구 톤

- 한국어 UI 문구는 짧고 직접적으로 씁니다.
- 사용자를 탓하는 표현을 피하고, 다음 행동을 알려줍니다.
- AI 결과는 확정된 정답이 아니라 수정 가능한 초안으로 표현합니다.

예시:

- `새 프로젝트 생성`
- `AI 아이디어 증강`
- `문서에 바로 적용`
- `AI 호출이 실패했어요 프로젝트에 대한 설명을 적어주세요`
- `실시간 동기화 중`

## 구현 메모

- 아이콘은 가능하면 lucide 아이콘을 사용합니다.
- 카드 안에 카드 형태의 중첩 UI를 만들지 않습니다.
- 대시보드와 프로젝트 홈은 landing page가 아니라 실제 작업 화면으로 시작합니다.
- 문서 편집 화면에서는 중앙 문서의 가독성을 최우선으로 두고, 보조 패널은 문서 영역을 침범하지 않게 합니다.
- 프로토타입에 있는 넓은 공백과 밝은 배경은 유지하되, 실제 구현에서는 반복 작업에 필요한 정보 밀도를 확보합니다.
